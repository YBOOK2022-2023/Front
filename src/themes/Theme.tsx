import { createTheme } from "@mui/material";

export const outerTheme = createTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#bd80fb",
    },
  },
});
